{
    "autoload": {
        "psr-4": {
            "Notary\\": "lib/"
        }
    },

    "autoload-dev": {
        "psr-4": {
          "Notary\\Tests\\": "tests/"
        }
    },

    "require": {
        "php": "^7.2 || ^8.0"
    },

    "require-dev": {
        "phpunit/phpunit": "^8.5",
        "ext-json": "*"
    },

    "scripts": {
        "test": [
            "./vendor/bin/phpunit --no-coverage"
        ]
    },
    
    "name": "aautar/notary",
    "description": "Library to compose and check against validation rules",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Avishkar Autar",
            "email": "aautar@fragmentspace.com"
        }
    ]
}
